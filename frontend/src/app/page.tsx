import { DashboardView } from "@/features/dashboard/components/dashboard-view";

/** Home / Today — the aggregated day view (GET /dashboard). */
export default function HomePage() {
  return <DashboardView />;
}

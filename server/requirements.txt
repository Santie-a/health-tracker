# Server (API gateway) dependencies. Install in a Python 3.12 venv:
#   py -3.12 -m venv .venv
#   .venv\Scripts\pip install -r requirements.txt
#
# Multi-arch friendly: all wheels resolve on arm64 (Pi5) too — no native build
# toolchain needed for FastAPI/SQLAlchemy/asyncpg/httpx.

# --- service ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9  # UploadFile / multipart form parsing

# --- database ---
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13

# --- image-svc bridge / http ---
httpx>=0.27

# --- tests ---
pytest>=8.0
pytest-asyncio>=0.23

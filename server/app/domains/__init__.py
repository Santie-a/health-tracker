"""Domain packages: telemetry, training, nutrition, recommendations.

Each domain is split into router (thin HTTP) · service (business logic) ·
repository (DB access) · models (SQLAlchemy) · schemas (Pydantic).
"""

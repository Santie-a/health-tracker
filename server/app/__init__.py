"""Health Tracker — API gateway (FastAPI).

The single backend the frontend talks to: DB access, recommendation engine,
Samsung Health ingestion, and a proxy to the GPU image service.
"""

__version__ = "0.1.0"
